# 姿勢崩れ通知アプリ（PostureGuard）

最終更新: 2026-04-12

## ステータス
**採用候補 → 5/7 MVPリリース目標**

---

## 概要

PCで作業中、Webカメラでリアルタイムに姿勢崩れを検出してアラートを出すWebアプリ。
ブラウザ内で完結するため、映像がサーバーに送られない（プライバシー安全）。

---

## ターゲットユーザー

- 大学院生・在宅エンジニア・デスクワーカー
- 自分自身が毎日使えるプロダクト

---

## 解決する課題

- 長時間のPC作業で姿勢が崩れていることに気づかない
- 気づいたときには肩こり・腰痛になっている
- 「姿勢を正そう」と思っても、崩れた瞬間に通知してくれるものがない

---

## 競合調査

- ePose、VisionPose: 医療・リハビリ向け。「PC常駐して通知」用途ではない
- tech neckアプリ（Electron+MediaPipe）: 海外に1件あり、日本語対応なし・UIが最低限
- **日本語対応のWebカメラ常駐型姿勢通知ツールは空白地帯**

---

## 技術設計

### アーキテクチャ
```
ブラウザ（クライアント完結）
├── Next.js + TypeScript
├── MediaPipe.js（骨格推定、WebAssemblyで動作）
├── 姿勢崩れ判定（角度計算＋基準値比較）
├── TailwindCSS（UI）
└── Recharts（姿勢スコアの時系列グラフ）

Supabase（ログ保存のみ）
Vercel（無料デプロイ）
```

### 処理フロー
```
カメラ映像
    ↓
MediaPipe.js → 骨格33点の座標を出力
    ↓
姿勢崩れ判定（ルールベース）
  - 頭部前方突出: 耳と肩のX座標ズレ
  - うつむき: 鼻のY座標低下
  - 前のめり: 顔のバウンディングボックス拡大
  - 肩の非対称: 左右肩のY座標差
    ↓
崩れスコアが閾値を5秒超えたらアラート
```

### キャリブレーション
起動時に「正しい姿勢で座ってください」→ 基準値を記録。
個人の体格差はこれで解消する。

### なぜPyTorchを使わないか
- 今回の判定は「骨格座標の角度・距離の比較」であり、ルールベースで十分な精度が出る
- PyTorchを追加してもユーザー体験は変わらない（自分のデータのみで学習 → 他人への汎化が落ちる）
- 面接で「なぜMLを使ったのか」と聞かれると答えに詰まる
- **シンプルな実装で十分な精度を出す技術判断力の方がポートフォリオとして評価される**

### 計算リソース
- MediaPipe.jsはノートPCのCPUで30fps動作
- GPU不要、CPU使用率15〜30%
- サーバーサイドの処理なし → Vercel無料枠で運用可能

---

## 技術スタック

| 要素 | 技術 |
|------|------|
| フロントエンド | Next.js + TypeScript |
| 姿勢推定 | MediaPipe.js（ブラウザ内） |
| UI | TailwindCSS |
| グラフ | Recharts |
| DB | Supabase |
| デプロイ | Vercel |

FastAPI・Pythonサーバー不要。

---

## MVPスコープ（5/7リリース）

### Must（必須）
- [ ] Webカメラ映像取得
- [ ] MediaPipe.jsで骨格表示
- [ ] キャリブレーション（基準姿勢登録）
- [ ] 姿勢崩れ検出 + アラート音
- [ ] Vercelデプロイ

### Should（あると良い）
- [ ] 姿勢スコアの時系列グラフ
- [ ] 崩れの種類を分類して表示（前のめり / うつむき / 肩崩れ）
- [ ] Supabaseでログ保存

### Won't（MVP後）
- [ ] スマホアプリ化
- [ ] PyTorchモデルによる分類（技術的に必要なら追加）
- [ ] Stripe課金

---

## 実装スケジュール

| 期間 | タスク |
|------|--------|
| 4/14〜15 | MediaPipe.jsでWebカメラ→骨格表示の動作確認 |
| 4/16〜18 | 姿勢崩れ判定ロジック（角度計算+基準登録） |
| 4/19〜21 | アラート機能+UIの基本実装 |
| 4/22〜25 | Supabaseでログ保存+グラフ表示 |
| 4/26〜28 | デザイン整え+バグ修正 |
| 4/29〜5/1 | Vercelデプロイ+ランディングページ |
| 5/2〜7   | バッファ+発表資料作成 |

---

## ポートフォリオとしての価値

**強み:**
- Next.js + TypeScript + Supabase を一気通貫で実装した証明
- 「ブラウザ内でリアルタイムAI推論」= サーバーレスアーキテクチャの理解を示せる
- 毎日自分で使っているプロダクト（説得力が高い）
- GitHubのREADMEにデモGIFを載せれば視覚的に映える

**Safie Tech Buildでの発表トーク:**
「カメラ映像をリアルタイムでAI処理して物理世界の課題を解決する、
という点でセーフィーの事業と同じアプローチです。
今回はWebカメラと姿勢推定で実装しました」

---

## 商用化

- 買い切りアプリ（1,000〜2,000円）or 月額サブスク
- MVP後に判断する。まずはOSSとしてGitHub公開

---

# 開発モード設定

## モード定義

2つのモードがある。**デフォルトは教育モード**。

### 教育モード（デフォルト）
学習と実装を同時に進める。理解を確認しながらコードを書く。

### 実装モードに切り替える方法
「実装モード」「急いでいる」「教育モードOFF」のいずれかを言う。
→ その会話セッション中は説明なしで高速実装に切り替える。

### 教育モードに戻す方法
「教育モードON」「学習モードに戻して」と言う。

---

## 教育モードのルール

### コードを書くときに必ずやること

1. **新しい概念・構文が出てきたら1行で説明する**
   - 言語・フレームワークを問わず適用する
   - 例: 新しいhook、デコレーター、型構文、ライブラリのAPI等
   - コードの直後にコメントか短いテキストで添える

2. **コア機能の実装前に日本語で条件を書かせる**
   - 「まずこの処理を日本語で説明してみて」と投げる
   - 答えられたらコードに落とす
   - 答えられなければ一緒に整理してからコードに進む

3. **実装後に「最も重要な1行」を示す**
   - なぜその1行が核心なのかを1文で説明する

4. **エラーが出たら直す前に意味を説明する**
   ```
   ① エラーメッセージの意味を1行で説明
   ② 原因の候補を2〜3個列挙
   ③「どれだと思う？」とユーザーに聞く
   ④ 特定できなければ一緒に絞り込む
   ⑤ 修正コードを渡す
   ```

5. **実装セッションの終わりに学習ログを促す**
   「今日出てきた新しいパターンを1つ言える？」と聞く。
   言えたら次に進む。言えなければ一緒に振り返る。

### やらないこと
- 説明なしにコードだけ渡さない
- ユーザーが理解していない状態で次の実装に進まない
- ボイラープレート以外をコピペ前提で渡さない

---

## 実装モードのルール

- 説明は省略。コードのみ渡す
- エラーは即修正。原因説明は不要
- 速度最優先

---

## コア機能とボイラープレートの区別

### コア機能（ユーザー自身に考えさせる）
プロジェクトの本質的なロジック・判断。
- このプロジェクトの場合: 姿勢崩れの判定条件、アラートのUX、キャリブレーションのフロー

### ボイラープレート（AIが担当してよい）
設定・接続・初期化など定型的な処理。
- このプロジェクトの場合: Next.js初期設定、Supabase接続、TailwindCSS設定、import文

---

## 技術スタック（参照）

- Next.js 14+ (App Router)
- TypeScript
- MediaPipe.js（@mediapipe/tasks-vision）
- TailwindCSS
- Supabase
- Vercel（デプロイ）

## このプロジェクトで必ず出る重要パターン

| パターン | いつ出るか | 説明すべき内容 |
|---------|-----------|--------------|
| `useRef<HTMLVideoElement>` | カメラ映像のDOM参照 | refとstateの違い |
| `useEffect(() => {}, [])` | MediaPipe初期化 | 依存配列の意味 |
| `useState<number>` | 姿勢スコア管理 | 再レンダリングの仕組み |
| `interface PoseLandmark` | 座標データの型 | なぜ型をつけるか |
| `requestAnimationFrame` | 毎フレーム処理 | setIntervalとの違い |

これらが初めて出てきたときは必ず解説する。2回目以降は省略してよい。
