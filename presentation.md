# Safie Tech Build 発表原稿 — PostureGuard

> 流れ：オープニング → ユーザーと課題（40秒）→ 技術選定（50秒）→ デモ＋成果物（20秒）→ FB依頼（10秒）

---

## オープニング（5秒）

**「PostureGuardという、Webカメラで姿勢崩れをリアルタイム検出して通知してくれるブラウザアプリを作りました。」**

---

## 1. ユーザーと課題（40秒）

### 必ず言う

**「ターゲットは、リモートワークや在宅で長時間PCに向かう人です。自分自身も大学院の研究で1日8時間以上PCに向かっていて、気づいたら肩が前に出て、首が突き出した猫背になっています。」**

**「困っているのは"崩れてから気づくまでの時間"です。集中していると姿勢が崩れていること自体に気づかない。気づいたときには、もう肩こりや腰痛が始まっている。「姿勢を正そう」と意識はしていても、崩れた瞬間に教えてくれるものがない、という状況です。」**

**「既存ツールを調べると、医療・リハビリ向けの本格的な姿勢解析サービスはあるんですが、"PCに常駐して、崩れた瞬間に通知する"という日常使い向けのものがほぼ空白地帯でした。海外に1件あった程度で、日本語対応のものは見つけられませんでした。」**

### できれば言う
- 自分が毎日使えるものを作るのが最低条件にした。使い続けないと姿勢改善は意味がないので。
- 「集中を切らない程度の弱い通知」が重要で、強い通知だと作業を中断してしまう。

---

## 2. 技術選定（50秒）— **ここが本題**

### 必ず言う

**「スタックはNext.js + TypeScript + MediaPipe.jsです。最大のこだわりは"バックエンドを持たず、ブラウザ内で全部完結させる"ことです。」**

**【なぜブラウザ完結にこだわったか】**
**「理由は3つあります。1つ目、プライバシー。カメラ映像を絶対にサーバーに送りたくなかった。2つ目、レイテンシ。30fpsで毎フレーム姿勢判定する必要があるので、サーバー往復していたらリアルタイム性が失われる。3つ目、コスト。サーバーレスならVercel無料枠で運用でき続けられる。この3つを成立させる手段がブラウザ完結でした。」**

**【MediaPipe.js について】**
**「姿勢推定にはGoogleが出しているMediaPipe.jsを使いました。これはWebAssembly（WASM）でブラウザ内で動く骨格推定ライブラリで、CNNベースの軽量モデル（pose_landmarker_lite、約7MB）が体の33点のランドマーク座標を毎フレーム返してくれます。」**

**「TensorFlow.jsのMoveNetやBlazePoseも候補にあったんですが、MediaPipeを選んだ理由は2つです。」**
- **「1つ目、Googleが公式でブラウザ向けWASMランタイムとモデルファイルをCDN配信していて、自分のサーバーにモデルを置かなくても、scriptタグを読み込むだけですぐ動く。"CDN"はContent Delivery Networkのことで、世界中に分散配置されたサーバーから配信されるのでロードも速い。」**
- **「2つ目、CPUだけで30fps動作する。GPUなしのノートPCでもCPU使用率は15〜30%程度で済む。」**


**【Next.js について】**
**「フレームワークはNext.jsを選びました。理由は複数あって、まずApp Routerでファイルベースのルーティングが設定なしで使える。ホットリロードがあり、保存するとブラウザが即座に反映してくれるので開発が速い。後でSupabase連携でAPIが必要になったときも、同じプロジェクト内にAPI Routesとして書ける。さらに最終的にVercelにデプロイする前提なので、最も親和性が高いという理由もあります。」**

**【TypeScript について】**
**「TypeScriptを選んだのは、骨格座標の33点や設定オブジェクトなど、扱うデータ構造が多かったからです。JavaScriptだとプロパティ名を1文字でも間違えるとランタイムまで気づきませんが、TypeScriptなら保存した瞬間にエディタが教えてくれる。リアルタイム処理で実行時エラーが出るとアプリが固まるので、開発時に潰せる設計の方が安全だと判断しました。」**

### できれば言う

**「機械学習のアプローチについて補足すると、姿勢崩れの"判定ロジック自体"はPyTorchなどを使わず、ルールベースで実装しています。MediaPipeが返してくれる骨格座標を、キャリブレーション時の基準姿勢と比較するだけです。」**

**「理由は、今回の課題は"その人自身の正しい姿勢から、どれだけ崩れたか"を見るものなので、骨格の角度・距離の比較で十分な精度が出るからです。MLを使うと"誰のデータで学習するか"問題が出てきて、汎用モデルを使えば既存サービスと差別化できず、個人データで学習すれば他人に汎化しない。シンプルな実装で十分な精度を出す方を選びました。」**

---

## 3. 成果物の現状＋デモ（20秒）

### 必ず言う（デモを見せながら）

**「では実際に動かしてみます。」**

**【画面共有でデモ】**

1. **「まず、正しい姿勢で座って"姿勢を記録"ボタンを押すと、基準値が登録されます。」**
2. **「骨格は鼻・耳・肩・腰など33点を毎フレーム検出していて、緑の点で表示しています。」**
3. **「ここで意図的に姿勢を崩します。」**
   - **猫背になる →** **「鼻のY座標が下がるので"うつむき"判定」**
   - **顔を近づける →** **「左右の耳の距離が広がるので"前のめり"判定」**
   - **片方の肩を下げる →** **「肩のY座標差で"肩崩れ"判定」**
4. **「崩れた状態が5秒続くとビープ音が鳴ります。一瞬の動きでは鳴らないようにしています。」**

**「動いているのはここまでで、ローカルでは全機能動作中ですが、Vercelへのデプロイがまだなので、現状は自分専用で運用しています。」**

### できれば言う
- 履歴グラフで「1時間あたりの崩れ回数」を時系列で見れる
- タブを切り替えても検出が止まらないようにDocument Picture-in-Picture APIで小窓表示している
- 設定や履歴はlocalStorageに保存している

---

## 4. 今日ほしいフィードバック（10秒）

### 必ず言う（課題の切り方に寄せる）

**「今日のフィードバックを1つに絞ります。このアプリのターゲットは"自宅で長時間PC作業をする人"で、これは僕自身であり、おそらくこの場にいる皆さんご自身でもあります。」**

**「だからこそ、当事者として率直に聞きたいです。"自分が毎日使うなら"という前提で、こういう機能があれば使い続ける、逆にこれがないと使えない、というアイデアをいただきたいです。」**

### できれば言う（具体例で誘導）
- 通知タイミング（リアルタイム vs まとめて）はどちらが続けられそうか
- 「姿勢崩れ」以外に拾ってほしい在宅作業の課題はあるか（目の疲れ・水分補給リマインドなど）
- どんな見た目・どんな置き場所なら「邪魔にならない」と感じるか

---

---

# 想定Q&A

## 技術・実装の深掘り

### Q. 判定は重くないのか？PCのリソース食わない？
**A.** WASM版MediaPipeのliteモデルを使っていて、Intel Core i7のノートPCでCPU使用率15〜30%程度です。30fpsで動きます。負荷の大半はMediaPipeの推論で、その後のルール判定（座標の引き算と閾値比較）はほぼコストゼロです。さらにカメラ解像度を640×480に明示的に制限することで、毎フレームのデータ量を抑えています。

### Q. 各機能はどのように実装しているか？

**【骨格検出】** MediaPipeの`PoseLandmarker.detectForVideo()`を毎フレーム呼び出して、33点のランドマーク座標（正規化された0〜1のx,y座標）を取得しています。

**【姿勢崩れ判定】** キャリブレーション時に「鼻のY座標」「左右耳間距離」「左右肩のY座標差」の3つを基準値として記録。毎フレームこれと比較して、閾値を超えたら崩れと判定します。

**【検出感度】** 「ゆるめ・普通・厳しめ」の3段階を用意していて、内部的には3つの指標それぞれの閾値テーブルを切り替えています。例えば「うつむき」判定の鼻Y座標差は、ゆるめが0.08、普通が0.05、厳しめが0.03です。値が小さいほど早く検出されます。

**【履歴保存】** localStorage に保存しています。サーバーに映像も座標も送らないというプライバシー方針との一貫性から、現状はクライアントローカルに閉じています。データ構造は `{"YYYY-MM-DD": { 種類別カウント, 動作時間ms }}` の日付ハッシュです。

**【Picture-in-Picture】** Chrome 116以降のDocument PiP APIを使っています。通常のVideo PiPと違って、映像 + 骨格描画canvas + UIをまるごと小窓に移せます。PiPウィンドウ内のrequestAnimationFrameはタブが非表示でも動き続ける仕様なので、タブを離れても姿勢検出が止まりません。

**【アラート音】** WebAudio APIで440Hzのサイン波を0.5秒生成しています。音声ファイル不要で、ビルドサイズも増えません。

### Q. 検出感度はどうやって緩めているのか？
**A.** 上の通り、3つの指標それぞれの閾値（鼻Y座標差・耳間距離倍率・肩Y座標差）を3段階のテーブルで切り替えています。「ゆるめ」は値を大きく取って崩れを検知しにくく、「厳しめ」は小さく取って早めに検知する設計です。判定ロジック自体は変えず、閾値だけ切り替える形なので、コードがシンプルに保てます。

### Q. なぜ履歴を localStorage に？Supabaseとかは？
**A.** プライバシー方針との一貫性です。映像も座標もサーバーに送らないと言っている以上、履歴データも極力ローカルに閉じたい、と判断しました。ただし、後述しますが端末間共有のニーズが出れば、Supabaseでクラウド保存への移行は予定しています。

### Q. 5秒継続というしきい値の根拠は？
**A.** 試行錯誤の結果です。1〜2秒だと一瞬の前傾（タイピング中に体を寄せる等）で誤発報が多く、10秒だと崩れが定着してしまう。自分で1週間使った感覚で5秒に落ち着きました。ここはユーザーごとに調整可能にすべきかもしれません。

---

## 精度・課題

### Q. 正面から見ているだけで猫背は検出できるのか？
**A.** 完全な精度ではないです。正面カメラでは「前後方向の傾き」が直接見えないので、間接的な指標で代替しています。「うつむき」は鼻のY座標の下降、「前のめり」は耳間距離の拡大（カメラに顔が近づくと耳間距離が画面上で大きく見える原理）で検出しています。本当に正確に猫背を見るなら横からカメラを置くのが理想ですが、デスク横にカメラを置く運用は現実的ではないので、正面の限られた情報で最大限拾う設計にしました。

### Q. 自分の体格・姿勢に偏った設計になっていないか？
**A.** その通りで、これは課題です。閾値設定は自分の使用感で決めているので、極端に体格や座り方が違う人だと再調整が必要かもしれません。ただ、キャリブレーション（個人ごとの基準値登録）でこの差をある程度吸収できる設計にはしています。

### Q. 使ってみてどうか？効果はあったか？
**A.** 体感ですが、姿勢を意識する頻度は明らかに上がりました。特にビープ音が鳴ったときの「あ、また崩れていた」という気づきは、自分一人では絶対に得られなかったものです。一方で、集中して作業しているときの通知が逆に集中を切る面もあって、感度設定や通知方法のチューニングはまだ必要だと感じています。

---

## 今後・展開

### Q. 今後の予定は？
**A.** まずVercelにデプロイして、自分以外も使える状態にします。次に、Supabaseでクラウドにデータ保存できるようにして、複数端末間の履歴共有を実現したいです。例えば自宅と研究室の両方で作業する人が、どちらの環境でも継続的に履歴を見られると便利なので。現状の自分専用 → 公開 → 複数端末対応、の順で広げる予定です。

### Q. デプロイしないのか？
**A.** これからします。技術的には Vercel に push するだけなのですが、まず自分でしっかり使い込んで、機能とUIを安定させてから公開しようと考えていました。発表が一区切りになるので、近いうちに公開する予定です。

### Q. 商用化は？
**A.** 余地はあると思っています。買い切り1,000〜2,000円か、サブスクか、まだ決めていません。まずはOSSとしてGitHubに公開して、反応を見てから判断します。

### Q. Safieの事業との接点は？
**A.** 「リアルタイムでカメラ映像をAI処理して、物理世界の課題を解決する」というアプローチが共通しています。セーフィーは屋外・現場向けでクラウドAI、このプロジェクトはPCカメラとブラウザ内推論、という違いはありますが、リアルタイム映像解析を身近な課題に応用する発想は同じだと考えています。

### Q. スマホ対応は？
**A.** 技術的にはモバイルChromeでも動きますが、Document PiP APIがモバイル未対応なので「タブを離れても動く」のメリットが消えます。MVPでは優先度を下げています。
